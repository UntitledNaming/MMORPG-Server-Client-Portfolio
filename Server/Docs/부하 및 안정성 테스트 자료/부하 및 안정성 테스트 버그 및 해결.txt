1. LoadDBData에서 InventoryItemLoad에서 InsertItemToSlot할때 이미 인벤토리에 아이템이 있음.
- 일단 ITEM_UID도 이상함 600이 세팅되어 있는데 들어온게 600000임.
- 아이템 갯수도 6개가 나와야 하는데 2개만 가지고 있음.
- 이거 게임 서버가 DB연결을 노트북에 넣은 worlddb에 연결했음.
- DBConfig IP가 노트북 IP로 설정되어서 그럼.

2. ItemPickUp 할때 GetConsumableItem에서 randomStatType이 default가 나오면서 중단점
- 지금 드랍 아이템이 애초에 randomStatCount가 0이어야 하는데 1이 나옴.

    FieldDropItem* pDropItem = m_dropItemPool->Alloc();
    pDropItem->dropUID = m_dropUIDAllocator++;
    pDropItem->expiredTime = 0;
    pDropItem->itemID = itemData->itemID;
    pDropItem->location.xpos = dropLocation.xpos;
    pDropItem->location.ypos = dropLocation.ypos;
    pDropItem->location.zpos = dropLocation.zpos;
    pDropItem->sectorPos.SetPos(SectorPos(SectorPos((dropLocation.xpos - FieldConst::MAP_WORLD_OFFSET_X) / FieldConst::SECTOR_SIZE, (dropLocation.ypos - FieldConst::MAP_WORLD_OFFSET_Y) / FieldConst::SECTOR_SIZE)));
    for (int i = 0; i < FieldDropItemConst::FIELD_DROP_ITEM_RANDOM_STAT_MAX; i++)
    {
        pDropItem->randomStat[i] = {};
    }
- 지금 FieldDropItemPool에서 다 초기화 하는데 딱 하나 randomStatCount를 초기화 안함. 여기서 걸린듯 함.


3. 몬스터 생성, 삭제시 문제
- 더미에서 Delete Monster 패킷 받을 때 이 MonsterID가 더미에 없는 ID 인 상황과 Create Monster 패킷 받을 때 이 MonsterID가 
  이미 있는 ID인 상황이 검측됨.
- 지금 몬스터가 생성, 삭제 되어야 하는 상황을 체크해서 이때 제대로 보내고 있는지 체크하고
- 섹터에 같은 몬스터가 중복으로 들어가 있거나 하는 상황으로 인해 나올 수도 있음.
- 기존 로직은 기존 섹터나 newSector나 주변에 9섹터가 있으면 정상 작동하는데 만약 섹터 좌표가 0,0 같이 경계에 있게 되면 문제가됨.
- bool 배열 index에 겹쳐지면 그 원소를 true로 했는데 이건 섹터 경계에 있게 되면 out 파라미터에 넘길 때는 0이 좌상단이 아니라 
 가운데 좌표가 되기 때문에 위치 불일치가 발생함.
- 따라서 지역에 std::pair<int16,int16>을 받는 set을 만들어 여기에 저장하고 find로 찾아서 있는지 체크하게 함.
- 근데 이 버그는 애초에 섹터 경계에 유저나 몬스터가 없기 때문에 이 문제는 아님.

------------------------------------------------------------------------------------------------------------------------------
- 우선 Delete Monster 패킷을 보냈을 때 모르는 ID라는 상황을 분석해보자.
- 기본적으로 Create Monster 상황인데 안한것인데... Create Monster 해야 하는 상황이 캐릭터가 섹터 업데이트 되거나
  몬스터가 섹터 업데이트 되거나, 캐릭터가 필드 진입하거나, 몬스터가 리젠될 때임.
- 솔직히 로직 상 버그인데 로직을 다시 봐도 일단은 모르겟음.
- 그러면 이를 찾기 위한 방법을 생각해보자. 즉, 서버에서 같은 몬스터 ID에 대해서 Create를 2번 보내거나 Delete를 2번 하거나
  하는 상황을 잡아보자. 이를 잡기 위해서 우선 유저 객체마다 전체 몬스터 ID를 index로 하고 bool 담는 배열을 하나 두고 해당 몬스터 ID에 대해서
  Create할때 bool을 true로 하고 Delete할때 false로 하려고 했는데 이건 좀 중단점은 잡는데 결국 이 유저에게 몬스터 생성, 삭제 패킷
  보내는 history 자체는 모름. 가장 최근의 한 행동만 아는 방식임.
- 그래서 배열이 int 타입 보다는 각 몬스터 ID에 대해서 Create, Delete history를 알 수 있게 원소가 int가 아니라 std::vector를 넣을 것임.
  이때 타입은 create, delete 타입과 이걸 한 위치를 표시하는 값 2개를 들 수 있게 구조체 형식으로 만들어서 넣을 것임.


- 지금 이제 중복 처리 1번 사진을 보면 지금 MonsterUpdate에서 리젠 후에 Create할때 버그가 발생함.
- 지금 history를 보면 제일 처음은 CharacterSectorUpdate할 때 create, monster가 sectorupdate하면서 delete, 섹터로 한번 더
  create, delete하고 나서 그 다음에 create_CharacterSectorUpdate하는데 섹터 옮기면서 몬스터 생성하고
- 그 다음이 리젠으로 인해 몬스터가 생성되는 거임.
- 즉, 저 사이에 몬스터가 죽음으로 인해 Delete 메세지를 보내야 하는데 그게 없음.
- 그래서 보니까 몬스터가  죽고나서 패킷 보낼때 몬스터가 있는 섹터에만 여러번 DELETE 패킷을 보내니 중복 DELETE가 나오고 
  주변 섹터에는 DELETE를 안뿌리니 만약 리젠되면 CREATE가 2번 되버릴 수 있음.


- 바꾸고 나서 다시 해보니 Delete가 2번 발생함.
- 지금 히스토리 마지막이 BaseAttack으로 Delete 되었는데 그 다음에 SkillUse할때 크래시남.
- 변경할때 m_Around[count]로 해야 하는데 m_Around[i]로 해버림.

4. 싱크 발생 시 서버 좌표로 맞추는데 굳이 이 유저의 주변 섹터에 싱크 틀어진 유저 좌표의 서버 좌표를 보내줄 이유는 없어 보임. 
- 어차피 틀어졋을 때 서버 좌표를 가지고 주변 유저에게 전파하고 틀어짐 확인했을 때 서버 좌표를 믿기 때문에 한번 더 보내줄 필요는 없음.

5. RST 보내는 상황에서 테스트 해보니 BadValue가 더미에서 측정됨
- ATK 랜덤 값이 지금 노멀이 1 ~ 3, 2 ~ 4인데 더미에서는 13 , 26, 29 이렇게 측정됨. 그리고 DB에도 atk가 17인게 보임.
- 이 프로토콜은 장비 아이템을 득템했을 때 서버가 보내주는 프로토콜에서 탐지된 것임.
- 그래서 우선 장비 아이템이 생성되는 경로를 따라 가보자
- 일단 드랍 아이템 생성하는 쪽 자체에는 문제가 없음.
- 그래서 응답 프로토콜 보낼때 atk, def가 범위 초과할때 중단점 때리고 상태를 봐보자.
- 그런데 지금 이때 안잡히고 지금 에러 터질때 DB도 확인하는데 발생 characterUID에서 애초에 ATK 넘는게 저장이 안되어 있음.

- 에러 로그를 보니 정확하게는 캐릭터가 생성될때 인벤토리나 , 장비 탭 아이템을 보내주는데 그걸 더미가 확인했을 때 에러난 것임.
- 그래서 CUserItemStorage에서 LoadItemFromDB할때 체크해봄.
- 다시 돌려보면 지금 DB 자체에 잘못 저장되어서 에러 바로 터짐.
- 그래서 이게 DB에 저장될 때를 봐야 할듯함. 지금 보면 계속 무기쪽 아이템에서 에러가 남.
- 지금 그러면 장비 아이템을 획득했을 때의 로직을 살펴봐야 함. 특히 DB에 Job던지 쪽.
- Job던지는 쪽에서 pJob가지고 체크하니 바로 걸림.
- 보니 max_hp일때 pJob의 maxHP에 저장해야 하는데 ATK에 저장하고 max_mp일때는 pJob의 def에 저장하고 있음.
  그래서 에러 체크함.


6. rtt 괜찮은데 싱크가 확 터지길래 뭔가 했더니 서버 프레임이 진짜 느리게 돌기도 했고 서버가 디버그용 변수 때문에 
  메모리를 너무 많이 먹고 있어서 페이지 폴트가 많이 발생해서 처리가 느리게 된 것으로 보임.
- 메모리 넉넉하게 했더니 발생x

< DB 스레드 처리 시간 추가 폴더 .csv>
7. 2500명 테스트 해보니 중간에 DBQueue Count가 12만 가까이 솟음. 그 후 점진적으로 낮춰짐.
- 우선 원인 자체는 쿼리문 1개를 처리하는데 DB에 저장하는데 너무 오래 걸린듯 함.
- 그래서 DB Job마다 Histrogram을 만들어서 이것도 저장해서 다시 테스트 해봐야 할듯
- 그리고 저장 큐 크기도 모니터링 해봐야 할듯. Job Count는 어차피 모니터링 스레드가 1초마다 밀어버리니
  부하를 제대로 관측하기 힘듬.

- 지금 쿼리문 각각에 대해서 큐 갑자기 터지기 시작할때의 처리시간을 보는데 몇초씩 걸리는 건 안보임.
- 그리고 지금 방식에서 0.5% 상위에 몇초씩 걸린게 몰려있으면 엑셀에 안찍힘.
- 지금 쿼리 시간이 순간적으로 오래 걸려서 그런것이라고 보기에는 너무 오랫동안 큐가 소비가 안됨.
- 그러면 이건 진짜 DB 스레드가 느리게 돈것으로 봐야 하는데 지금 프로세스 CPU 사용률은 괜찮아 보임.
- 그러면 전체 CPU 사용률을 봐야 함.
- 또한 지금 DB 스레드에서 어디에 부하가 걸리는지 분리해서 측정해야 함.
- cpu 사용량도 늘어나지 않고 execute 제외 다른 구간도 시간 자체가 오래걸리는게 아님.
- 지금 Execute의  Count 갯수를 유의미 하게 봐야 하는데 

65536	524288	1048576	2097152	4194304	2908	128	256	256	256	256	22	128	256	256	512	1024	2886	210
65536	2097152	4194304	4194304	8388608	1207	128	256	256	256	512	11	128	256	512	512	1024	1196	2076
65536	2097152	4194304	4194304	8388608	1108	128	256	256	256	256	11	256	256	512	1024	1024	1097	4100
65536	2097152	2097152	4194304	4194304	1333	256	256	256	256	256	11	128	256	512	512	512	1322	5948
- 맨 마지막이 db큐 카운트 6번째 컬럼이 execute Count 갯수임. 지금 급증할때 딱 Execute Count 갯수도 확 줄어들음.
- 이게 무슨 말이냐면 쿼리 처리 시간 자체가 늘어났기 때문에 1초마다 측정한 Count 갯수가 확 줄어든 것임.

65536	2097152	4194304	4194304	4194304	1916	0	0	0	0	0	0	128	256	512	512	1024	1916	160895
65536	2097152	2097152	4194304	4194304	1524	0	0	0	0	0	0	128	256	512	1024	1024	1524	161438
65536	2097152	2097152	4194304	4194304	2032	0	0	0	0	0	0	128	256	512	512	1024	2032	161464
65536	2097152	2097152	4194304	4194304	2109	0	0	0	0	0	0	128	256	512	512	1024	2109	161424
65536	2097152	2097152	4194304	4194304	2134	128	256	256	256	256	15	128	256	512	512	1024	2119	161360
65536	2097152	2097152	4194304	8388608	2235	128	256	256	256	256	29	128	256	512	512	512	2206	161250
65536	2097152	2097152	4194304	8388608	2336	128	256	256	256	256	24	128	256	512	512	2048	2312	161019
65536	2097152	2097152	4194304	4194304	2388	0	0	0	0	0	0	128	256	512	512	1024	2388	160761
- 이게 지금 16만에서 DB 큐 갯수가 줄어 드는 구간에서의 Execute Count 갯수임.
- 보면 DB 큐 갯수가 줄어들면서 지금 Execute 갯수가 늘어나고 있음.

- 그러면 기존에 측정한 DBJob 쿼리 처리 시간을 봐서 어떤게 시간이 오래걸린지 보자.
- 지금 각 쿼리 처리 시간도 측정해서 보는데 CharacterProgrss가 count가 가장 많고 처리시간이 4배가 늘어남. 또한 itemcount도
  처리 시간 자체가 4배정도 올라가는데 평균 적인 시간 자체는 itemcount 처리시간이 높음. 대신 count가 적음.
- 그리고 유의미한 지표로 보이는게 큐가 소비되는 시점 근처에 ItemCount에 대한 처리가 이제 거의 없음. 뭐냐면 즉, ItemCount
  처리하던 처리 시간이 증발함으로 인해 큐가 더 빠르게 소모 될 수 있다는 것임.
- 단순히 CharacterProgressJob 갯수가 문제가 아님. 부하 감소하는 구간을 보면 저 ItemCountJob이 사라짐으로 인해 처리할 
  능력이 상승하면서 이 구간에서 CharacterProgrssJob 처리 시간도 솔직히 부하 증가할때랑 크게  차이는 없고 오히려 Count는 훨씬
  증가함. ItemCountJob 처리 시간이 사라지니 CharacterProgressJob 처리 할 시간이 늘어난 것임.
- 언제 터지는지도 보면 유저가 max치인 2500에 도달하고 나서 얼마 몇 초 안가 바로 터짐.


< 소모품 갯수 500개 설정 폴더>
- 그러면 소모품 다 못쓰게 maxStack 500개로 설정해서 테스트해보자.
- 테스트 결과를 보면 db 큐가 계속 늘어남.
- 각 Job에 대한 처리 시간이 순간적으로 모두가 다 같이 높아지는데 cpu 사용률도 그대로임.
- 그래서 DB의 한계에 다다른 것임.  DB가 결국 디스크에 IO하는것인데 그 처리 속도보다 게임 서버가 쿼리 보내서 쓰라고 하는 생성
  속도가 더 높은 것임.
- 우선 지금 시점에 개선해볼만한게 CharacterProgress의 횟수가 굉장히 높음. 그래서 이 횟수 자체를 근본적으로 줄여보자.

- 현재 관련 Job level과 curExp를 갱신하는 Job임. 그래서 유저 객체 멤버로 bool 플래그 하나두고 기본 false인데 gainexp를 하면 true로
  바꾸고 유저 객체 마다 일정 범위 내 랜덤 시간을 부여 해서 시간 되면 Job던지고 플래그 끄고 로그 아웃할때도 반영 하게 함.

<CharacterProgressJob Count 줄이기>
- 터지긴 하는데 확실히 CharacterProgressJob Count는 1000대에서 20 ~30대로 내려옴. 
- 이제는 서버에서 당장 뭐 개선해볼만한게 없음. 쿼리문 자체도 없고 아이템 카운트가 그나마 높은데
  솔직히 이렇게 DB 큐 터지는 상황이면 아이템 카운트를 똑같이 dirtyFlag로 바꿔도 터질 것임.
- 그래서 아예 지금은 Job 마다 트랜잭션 열고 쿼리 날리고 커밋하는데 여러 Job에 대해서 트랜잭션 열고 여러개 쿼리 날리고
  커밋하는 방식으로 바꾸는 것임.


- 이게 어떤 효능이 있냐면 트랜잭션 열어도 쿼리 날리면 DB 서버 메모리에 반영은 되는데 우리가 커밋을 해줘야지 우리가
  날린 쿼리에 대한 내용을 DB 서버 디스크에 로그 섹터에 저장을 함.(실제 테이블 데이터가 있는 디스크에 저장은 천천히)
- 그러면 지금 처럼 1초에 Job이 10개면 디스크에 10번 접근하게 됨. 그런데 만약 여러개의 Job에 대해서 한번만 트랜잭션 열고
  커밋하면 10개의 쿼리 날려도 메모리에만 반영되고 마지막 커밋할때 디스크에 딱 1번 반영되니 디스크 접근 횟수 자체가 다르니
  DB의 처리 부하가 줄어들 것임.
- write는 이렇게가고 read는 어떻게 동작하냐면 트랜잭션 열든 안열든 select 날릴때 db 서버 캐시하고 있는 영역에 없으면 디스크에
  접근해서 읽어오고 캐시 영역에 저장하고 나서 우리에게 줌. 커밋할때 우리에게 주는게 아님.
- 그래서 캐릭터 선택job에서 트랜잭션 열고 닫는 명령어를 던져줄 필요가 없음. 다만 만약 다른 쪽에서 DB와의 커넥션이 있어서
  캐릭터나 아이템  테이블 변경할 가능성이 있으면 트랜잭션이 필요함. 


< 여러개 Job 트랜잭션 묶어서 보내기>
- 

8. 몬스터 hp 및 캐릭터 hp가 badvalue
- 몬스터 hp는 3만 대가 나와버리고 캐릭터 hp는 음수가 나옴.
- 일단 추측은 언더 플로우가 발생한 것으로 보임.
- Damage 함수 내부 자체는 어차피 hp가 int16이니 언더 플로우 안나오니 단 하나는 매개인자로 들어온 damage가 문제임.
- CalDamage에서  damage가 특정 값 이상 나와서 언더 플로우 발생 체크될때 중단점 올리자.
- 에러 터졌을 대 atk, def를 확인해봤음. 그랬더니 장비 쪽의 atk, def가 음수가 뜸.
- 근데 지금 애초에 해당 유저의 아이템 테이블 보면 장비 탭에 장착된 장비 자체가 없음. usecount가 0이고 
 헬멧 부터 무기까지 다 id가 0임. 0번index는 초기화 안하고 있음.
- 그런데 애초에 0번 index에 이상한 값이 들어가면 안되는 상황임.
- 뭐로 인해 atk, def 등의 캐시값에 이상한 값이 들어간건지 체크하기 위해 우선 장비 장착, 해제 함수에서 캐시 값이 비정상값일때
  크래시 발생시키게 함.
- 애초에 Init할때 캐시 데이터 0으로 초기화가 안되니 get할때 가비지 값이 나옴
- 고치니 에러 안남.